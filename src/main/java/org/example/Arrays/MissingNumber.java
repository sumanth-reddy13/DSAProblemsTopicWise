package org.example.Arrays;

public class MissingNumber {
    public static void main(String[] args) {

    }
    /*
           Problem Link - https://www.geeksforgeeks.org/find-a-repeating-and-a-missing-number/

           -> Approach 1 - Use a freq Array to store freq of values in the array. Traverse the freq array, if any index freq is
                zero, then that is the missing value.

           -> Approach 2 - Sort the array and see which index + 1 != nums[index]

           -> Approach 3 - Using XOR approach. (1 ^ 2 ^ 3 ^ 4) ^ (1 ^ 2 ^ 4) = 3

     */

    // Problem - 2 -> Find the missing and repeating Number.

}
